#![allow(warnings, unused)]
use utils::*;

fn run(problem: i32, lines: &Vec<String>) -> i32 {
    0
}

fn main() {
    let input_lines = get_input_lines(file!());

//     let example1 = multiline_lines("
// ");

    run(1, &input_lines).debug("Star 1");
    aoc_exit();
}
